version: '3'
services:
  app:
    build: .
  environment:
    MARIADB_ROOT_PASSWORD: "rootpw"
    MARIADB_DATABASE: "mocword"
    MARIADB_USER: "moctane"
    MARIADB_PASSWORD: "pw"

  mariadb:
    image: "mariadb:10.6"
    environment:
      MARIADB_ROOT_PASSWORD: "rootpw"
      MARIADB_DATABASE: "mocword"
      MARIADB_USER: "moctane"
      MARIADB_PASSWORD: "pw"
    volumes:
      - "./mariadbdata:/var/lib/mysql"
